
# SPI ACPI Configuration

```shell


 Device (SPI1)
   {
     Name (_HID, "PRP0001")  // _HID: Hardware ID
     Name (_UID, 0x0)  // _UID: Unique ID
     Name (_CRS, ResourceTemplate ()  // _CRS: Current Resource Settings
     {
       QWordMemory (ResourceConsumer, PosDecode, MinFixed, MaxFixed, NonCacheable, ReadWrite,
         0x0000000000000000, // Granularity
         0x00000e001fe001f0, // Range Minimum
         0x00000e001fe001ff, // Range Maximum
         0x0000000000000000, // Translation Offset
         0x0000000000000010, // Length
         ,, , AddressRangeMemory, TypeStatic)
       Interrupt (ResourceConsumer, Level, ActiveHigh, Shared, ,, )
       {
         29,
       }
     })
     Name (_DSD, Package (0x02)  // _DSD: Device-Specific Data
     {
       ToUUID ("daffd814-6eba-4d8c-8a91-bc9bbf4aa301") /* Device Properties for _DSD */,
       Package (0x01)
       {
         Package (0x02)
         {
           "compatible",
           "loongson,ls2k1000-spi"
         },
       }
     })
     
     
     
     Device(FRAM)
     {
       Name (_HID,"SPT0001")
       Name (_CRS,ResourceTemplate()
       {
       SPISerialBus(0, PolarityLow, FourWireMode, 8,
                         ControllerInitiated, 1000000, ClockPolarityLow,
                         ClockPhaseFirst, "\\_SB.PCI1.SPI1",0x00,,)
       })
     }
   }
   
   
)
```

# 手动启动

## 龙芯机箱原生镜像

```
	load /dev/fs/ext3@wd0b/vmlinuz-4.19.0-19-loongson-3
	initrd /dev/fs/ext3@wd0b/initrd.img-4.19.0-19-loongson-3
	g console=ttyS0,115200 earlycon=uart,mmio,0x1fe001e0 loglevel=8
```

## 自己编译镜像
```shell

	load /dev/fs/ext3@wd0b/vmlinuz
	g console=ttyS0,115200  earlycon=uart,mmio,0x1fe001e0  loglevel=8
	
```

# 烧录pmon指令

```shell

fload /dev/fs/ext3@wd0b/gzrom.bin
```

# 更新LINUX系统

在编译内核并生成vmlinuz镜像时就将内核和rootfs打包在一起，更新vmlinuz 即是将OS更新，如果需要修改rootfs ，请使用buildroot构建新的rootfs，生成rootfs压缩文件的路径填写在linux 配置文件中，可以通过指令make menuconfig 直接添加，具体添加方式可以通过chatgpt查询。 

```shell

dfu /dev/fs/ext3@wd0b/vmlinuz
```

# 测试canfd指令

``` shell

	#设置canfd命令
	ip link set down can0
	ip link set up can0 type can bitrate 1000000 dbitrate 5000000 fd on
	ip link set up can0
	
	ip link set down can1
	ip link set up can1 type can bitrate 1000000 dbitrate 5000000 fd on
	ip link set up can1
	#设置can命令
	ip link set down can0
	ip link set up can0 type can bitrate 500000
	#性能测试
	time cangen can0 -f -b -g 1 -i 100 -L 64 -n 1000
	# 变速测试
	cangen can0 -f -b -g 1000 -i 100 -L 64 -n 1
	# 显示一下can信息
	ip -details link show can0

```

# 合并镜像




```shell
sudo apt install srecord

srec_cat gzrom.bin -binary  vmlinuz -binary -offset 0x200000 -o image.bin -binary  -fill 0xFF 0x0000 0x200000

srec_cat gzrom.bin -binary  vmlinuz -binary -offset 0x200000 -o image.bin -binary
```

# 测试jffs2

```shell
首先内核支持mtd,spi-nor,mtd block, jffs2 fs

# 格式化
flash_erase -j /dev/mtdx 0 0
#挂载
mount -t jffs2 /dev/mtdblockx /mnt


```

# 挂载debugfs

```
mount -t debugfs none /sys/kernel/debug
```

# JTAG

1、确保开发板断电，ejtag已和电脑连接
2、打开ejtag软件即：loongson-debugger  双击la_dbg_tool_usb.exe   会弹出终端
3、在终端中输入source configs/config.ls2k2000  点击回车
4、输入set 点击回车
5、给开发板上电    会返回寄存器状态，如果长时间没反应说明没有和芯片通讯上，此时如过确认板卡状态没有问题，请从1重新操作
 6、输入call program_cachelock image.bin    点击回车 此指令为烧录指令，第三个参数为要烧录的文件